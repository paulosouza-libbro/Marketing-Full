[build]
builder = "NIXPACKS"

[[services]]
name = "api"
source = "backend"
startCommand = "uvicorn api:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "frontend"
source = "frontend"
startCommand = "npm start"
